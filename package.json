{
  "name": "jsmp-infrastracture-magic-array",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "babel src -d build",
    "build": "npm run clean && npm run compile",
    "test": "npm run build && node build/test.js",
    "clean": "shx rm -rf build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.11"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.3.1",
    "shx": "^0.3.2"
  }
}
